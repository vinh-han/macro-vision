version: "2"

sql:
    - engine: "postgresql"
      queries: "./database/ddl/queries.sql"
      schema: "./database/ddl/schema.sql"
      gen:
          go:
              package: "database"
              out: "./database"
              emit_json_tags: true
